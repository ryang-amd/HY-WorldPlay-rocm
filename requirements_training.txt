# Training dependencies (install on top of base torch + ROCm environment)
# Usage:
#   pip install -r requirements_training.txt
#   pip install torchdata --no-deps   # separate to avoid torch version conflict

# Core training deps
cloudpickle>=3.1.0
remote-pdb>=2.1.0
wandb>=0.24.0
pyarrow>=14.0.0
datasets>=4.0.0
multiprocess>=0.70.0
dill>=0.3.0
xxhash>=3.0.0
aiohttp>=3.9.0

# torchdata must be installed separately with --no-deps:
#   pip install torchdata --no-deps
